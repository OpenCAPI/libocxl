# Disable built-in rules
MAKEFLAGS += -rR

AR  = $(CROSS_COMPILE)ar
AS	= $(CROSS_COMPILE)as
LD	= $(CROSS_COMPILE)ld
CC	= $(CROSS_COMPILE)gcc
NM  = $(CROSS_COMPILE)nm
OBJCOPY  = $(CROSS_COMPILE)objcopy
CFLAGS	+= -g -Wall -O2 -m64
TESTCFLAGS  += $(CFLAGS) -O0 -DTEST_ENVIRONMENT=1
